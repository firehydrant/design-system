import { colors } from '../../theme/colors.theme';
import { space } from '../../theme/space.theme';
import * as font from '../../theme/font.theme';

export const Text = {
  sizes: {
    1: {
      fontSize: `${font.fontSizes[1]}`,
      lineHeight: `${font.lineHeights[1]}`,
      fontWeight: `${font.fontWeights[1]}`,
      color: `${colors.grey[90]}`,
    },
    2: {
      fontSize: `${font.fontSizes[2]}`,
      lineHeight: `${font.lineHeights[2]}`,
      fontWeight: `${font.fontWeights[2]}`,
      color: `${colors.grey[90]}`,
    },
    3: {
      fontSize: `${font.fontSizes[3]}`,
      lineHeight: `${font.lineHeights[3]}`,
      fontWeight: `${font.fontWeights[3]}`,
      color: `${colors.grey[90]}`,
    },
    4: {
      fontSize: `${font.fontSizes[4]}`,
      lineHeight: `${font.lineHeights[4]}`,
      fontWeight: `${font.fontWeights[4]}`,
      color: `${colors.grey[90]}`,
    },
    5: {
      fontSize: `${font.fontSizes[5]}`,
      lineHeight: `${font.lineHeights[5]}`,
      fontWeight: `${font.fontWeights[5]}`,
      color: `${colors.grey[70]}`,
    },
    6: {
      fontSize: `${font.fontSizes[6]}`,
      lineHeight: `${font.lineHeights[6]}`,
      fontWeight: `${font.fontWeights[6]}`,
      color: `${colors.grey[70]}`,
    },
    7: {
      fontSize: `${font.fontSizes[7]}`,
      lineHeight: `${font.lineHeights[7]}`,
      fontWeight: `${font.fontWeights[7]}`,
      color: `${colors.grey[70]}`,
    },
  },
};
